[project]
name = "keabot-backend"
version = "0.1.0"
description = "KeaBot - Agente de Automação Local com ContextNavigator"
requires-python = ">=3.11"
readme = "README.md"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]
